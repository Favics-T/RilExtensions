import React from 'react'

const CardComponent = ({title,classname, description}) => {
  return (
    <div>
      <div>
        
        <h1>{title}</h1>
      </div>
    </div>
  )
}

export default CardComponent
